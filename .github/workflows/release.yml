name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Check
        run: node check.mjs && node pack.mjs --check

      - name: Require tag to match manifest
        run: |
          tag="${GITHUB_REF_NAME#v}"
          ver="$(node -p "JSON.parse(require('fs').readFileSync('manifest.json','utf8')).version")"
          test "$tag" = "$ver"

      - name: Pack
        run: node pack.mjs

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ver="${GITHUB_REF_NAME#v}"
          gh release create "$GITHUB_REF_NAME" \
            "authnudge-extension-v${ver}.zip" \
            --title "Authnudge ${GITHUB_REF_NAME}" \
            --notes "Install from the Chrome Web Store: https://chromewebstore.google.com/detail/authnudge/blcpeglclodcfkmakeilegdknbdaglpa (Add to Chrome). This zip is the store upload artifact. Contributors: Load unpacked from the repo for development."
